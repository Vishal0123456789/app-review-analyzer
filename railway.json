{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}",
    "restartPolicyCondition": "on-failure",
    "restartPolicyMaxRetries": 3
  }
}
